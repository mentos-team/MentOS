# =============================================================================
# USERSPACE SETUP
# =============================================================================

# Add the userspace subdirectories
add_subdirectory(bin)
add_subdirectory(tests)
add_subdirectory(examples)
